<UserControl x:Class="WPFAddressBook_2.MVVM.Views.ContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFAddressBook_2.MVVM.Views"
             xmlns:viewmodels="clr-namespace:WPFAddressBook_2.MVVM.ViewModels"
             xmlns:models="clr-namespace:WPFAddressBook_2.MVVM.Models"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ContactsViewModel}" 
             Height="440" Width="840">
    <Grid Margin="20 5 20 20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0 0 30 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Förnamn"/>
                <TextBox Name="tb_FirstName" Padding="3" Margin="0 0 0 10" Text="{Binding FirstName}" />

                <TextBlock Text="Efternamn"/>
                <TextBox Name="tb_LastName" Padding="3" Margin="0 0 0 10" Text="{Binding LastName}" />

                <TextBlock Text="Telefonnummer"/>
                <TextBox Name="tb_PhoneNumber" Padding="3" Margin="0 0 0 10" Text="{Binding PhoneNumber}" />

                <TextBlock Text="Email"/>
                <TextBox Name="tb_Email" Padding="3" Margin="0 0 0 10" Text="{Binding Email}"/>

                <TextBlock Text="Gatuadress"/>
                <TextBox Name="tb_SteetName" Padding="3" Margin="0 0 0 10" Text="{Binding StreetName}"/>

                <TextBlock Text="Postkod"/>
                <TextBox Name="tb_PostalCode" Padding="3" Margin="0 0 0 10" Text="{Binding PostalCode}" />

                <TextBlock Text="Stad"/>
                <TextBox Name="tb_City" Padding="3" Margin="0 0 0 10" Text="{Binding City}" />
            </StackPanel>

            <Button Name="btn_Add" Command="{Binding AddContactCommand}" Grid.Row="1" Content="LÄGG TILL" Style="{StaticResource addButton}" />
            <!--<Button Name="btn_Add" Command="{Binding AddContactCommand}" Grid.Row="1" VerticalAlignment="Bottom" Content="LÄGG TILL" Padding="5" Background="#78b869" BorderThickness="0" Foreground="White" FontWeight="DemiBold"/>-->
        </Grid>

        <Grid Grid.Column="1" Margin="30 0 0 0" Name="parent">
            <ListView Name="lv_Contacts" ItemsSource="{Binding Contacts}">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="models:ContactModel">
                        <StackPanel Margin="0 5 0 5" Width="355" Background="MistyRose">
                            <!--<StackPanel Margin="0 5 0 5" Width="{Binding ActualWidth, ElementName=parent}">-->

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="5 5 5 5">
                                    <TextBlock Name="tblock_DisplayName" FontWeight="Bold" Text="{Binding DisplayName}" />
                                    <TextBlock Name="tblock_Email" Text="{Binding Email  }" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" >
                                    <Button Name="Btn_Edit" Click="Btn_Edit_Click" Content="ÄNDRA" Margin="0 0 5 0" Padding="5" Grid.Column="1" VerticalAlignment="Center" Background="#78b869" BorderThickness="0" Foreground="White" FontWeight="DemiBold"/>
                                    <Button Name="Btn_Remove" Click="Btn_Remove_Click" Content="TA BORT" Grid.Column="1" Style="{StaticResource removeButton}" />
                                    <!--<Button Name="btn_Remove" Click="Btn_Remove_Click" Content="TA BORT" Margin="0 0 5 0" Padding="5" Grid.Column="1" VerticalAlignment="Center" Background="DarkSalmon" BorderThickness="0" Foreground="White" FontWeight="DemiBold"/>-->
                                </StackPanel>
                            </Grid>

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        
    </Grid>
</UserControl>
